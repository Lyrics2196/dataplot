[project]
name = "lyraplot"
version = "0.1.0"
description = "A matplotlib style package for creating publication-ready plots"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["matplotlib>=3.10.9", "numpy>=2.4.6"]
